<!DOCTYPE html>
<html lang="en" dir="ltr" class="h-100">

<head>
  <meta charset="utf-8" />
  <title>OAS</title>
  <link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.6.3/css/all.css"
    integrity="sha384-UHRtZLI+pbxtHCWp1t77Bi1L4ZtiqrqD80Kn4Z8NTSRyMA2Fd33n5dQ8lWUE00s/" crossorigin="anonymous" />
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet"
    integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous" />
  <link rel="stylesheet" href="/css/styles.css" />
</head>

<body class="d-flex flex-column h-100">
  <header>
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
      <div class="container-fluid">
        <% if (isLoggedIn==true) { %>
          <% if (accountType=="seller" ) { %>
            <a class="navbar-brand" href="/">OAS Seller</a>
            <% } else if (accountType=="buyer" ) { %>
              <a class="navbar-brand" href="/">OAS Buyer</a>
              <% } else if (userRole=="ADMIN" ) { %>
                <a class="navbar-brand" href="/">OAS Admin</a>
                <% } else if (userRole=="USER" ) { %>
                  <a class="navbar-brand" href="/">OAS</a>
                  <% } %>
                    <% } else { %>
                      <a class="navbar-brand" href="/">OAS</a>
                      <% } %>

                        <button class="navbar-toggler" type="button" data-bs-toggle="collapse"
                          data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent"
                          aria-expanded="false" aria-label="Toggle navigation">
                          <span class="navbar-toggler-icon"></span>
                        </button>
                        <div class="collapse navbar-collapse" id="navbarSupportedContent">
                          <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
                            <% if (isLoggedIn==true) { %>
                              <% if (userRole=="ADMIN" || accountType=="buyer" ) { %>
                                <li class="nav-item">
                                  <a class="nav-link" href="/">Listings</a>
                                </li>
                                <% } %>
                                  <% if (accountType=="seller" ) { %>
                                    <li class="nav-item">
                                      <a class="nav-link" href="/">My Listings</a>
                                    </li>
                                    <% } %>
                                      <% if (accountType=="buyer" ) { %>
                                        <li class="nav-item">
                                          <a class="nav-link" href="/my-bids">My Bids</a>
                                        </li>
                                        <% } %>
                                          <% } %>
                                            <li class="nav-item">
                                              <a class="nav-link" href="/help">Help</a>
                                            </li>
                                            <li class="nav-item">
                                              <a class="nav-link" href="/about">About</a>
                                            </li>
                          </ul>
                          <% if (isLoggedIn==true) { %>
                            <a class="btn btn-dark my-2 my-sm-0" href="/logout" role="button">Logout</a>
                            <% } else if (isLoggedIn==false) { %>
                              <a class="btn btn-dark my-2 my-sm-0" href="/login" role="button">Login</a>
                              <% } %>
                        </div>
      </div>
    </nav>
  </header>